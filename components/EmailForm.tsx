'use client'; import { useState } from 'react'; export default function EmailForm() { const [email, setEmail] = useState(''); const [submitted, setSubmitted] = useState(false); const handleSubmit = async (e: React.FormEvent) => { e.preventDefault(); await fetch('https://formspark.co/f/your-id', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ email }) }); setSubmitted(true) }; if (submitted) return <p className='text-green-600'>Спасибо!</p>; return ( <form onSubmit={handleSubmit} className='space-y-4'> <input type='email' required placeholder='Ваш email' value={email} onChange={(e) => setEmail(e.target.value)} className='w-full px-4 py-2 border border-gray-300 rounded-lg' /> <button type='submit' className='w-full bg-purple-600 text-white py-2 rounded-lg'>Получить доступ</button> </form> ) }